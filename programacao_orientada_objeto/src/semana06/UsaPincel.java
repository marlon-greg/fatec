public class UsaPincel {
    public static void main(String[] args) {

        Pincel p; // Reserva espaço para armazenar um objeto Pincel
        p = new Pincel(); // Cria o objeto e atribui valores iniciais

        // Pincel maiúsculo é CLASSE e o minúsculo OBJETO = INSTÂNCIA
        // O New é usado para a ação para criar o objeto
        Pincel pincel = new Pincel();
    


    }
}
